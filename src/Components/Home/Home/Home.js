import React from 'react';
import ReactSlider from '../../ReactSlider/ReactSlider'

const Home = () => {
    return (
        <div>
            <ReactSlider/>
        </div>
    );
};

export default Home;